Para criar novas variáveis de ambiente:

Adicione a variável com prefixo "VITE_" no arquivo .env
Adicione a variável no arquivo entrypoint.sh
Adicione a variável no docker-compose.yml
Adicione a variável no Dokerfile, na sessão de ARGs
